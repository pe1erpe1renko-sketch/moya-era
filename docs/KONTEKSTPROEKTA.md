# Контекст проекта «Моя Эра» — прочитать первым делом

Этот файл — память между сессиями. Если ты открыл репозиторий впервые, прочитай его целиком, прежде чем что-либо делать.

---

## 1. Что это за проект

Портал-агрегатор по эзотерике. Шесть систем самопознания: матрица судьбы, натальная карта, дизайн человека, нумерология, таро, совместимость. Флагман — **матрица судьбы**, остальные системы разворачиваются по её образцу.

Главная задача продукта — **удержание**. Заказчик формулирует так: «чтобы клиент возвращался постоянно, ежедневно». Сайт зарабатывает на ежемесячной подписке, а не на разовых продажах.

Позиционирование из брифа: **салон-обсерватория**. Салон — атмосфера и ритуал, обсерватория — настоящие расчёты с объяснением, откуда взялось каждое число. Максимализм в эстетике, минимализм в утверждениях.

**Продукт до нас.** Первую версию делала Александра в Lovable, потом перенесла на Next.js. Дизайн, тексты и архитектура данных у неё сильные — их сохраняем. Продукта за фасадом не было: авторизация подделана на localStorage, платежей нет, разборы не генерировались. Полный разбор её работы — в проекте заказчика, документ «Аудит и доработки».

---

## 2. Заказчик и как с ним работать

Пётр (RomanPETROS). Не программист. Ставит задачи голосом, мыслит продуктом и деньгами, в технике разбирается слабо — GitHub освоил в этой работе с нуля.

**Правила общения, которые он попросил явно:**

- **По одному шагу за раз.** Он выполняет, отчитывается, задаёт вопросы, и только потом следующий шаг. Не вываливать простыни инструкций.
- **Не забегать вперёд.** Не делать того, о чём не договорились. Он прямо сказал: «прежде чем ты сразу пересобираешь файлы — не надо, мы ещё не закончили».
- **Не тратить токены впустую.** Сначала обсудить, что делаем, получить подтверждение, потом делать.
- **Инструкции — как для новичка.** Конкретные кнопки, что увидишь на экране, а не «зайди в настройки».

Он ценит, когда ему объясняют «почему», а не только «что нажать». И заранее предупреждают о рисках.

---

## 3. Утверждённые решения (не пересматривать без него)

**Архитектура — уровневая модель.** Шесть систем на верхнем уровне, внутри каждой свои типы разбора. «Совместимость» — это тип разбора внутри системы, а не седьмая сущность: совместимость по матрице, синастрия по натальной карте, композит по дизайну человека. На главной остаётся как витрина.

**Единица оплаты — профиль человека.** Тариф задаёт, сколько людей открыто: Базовый — 1, Семейный — 5, Практик — без ограничений. Подписка открывает все системы и все типы разбора по датам своих людей. Цены в таблице `plans`, меняются без деплоя. Текущие цифры стартовые, для обсуждения.

**Кредиты — только на сообщения наставнику.** Это единственный расход, растущий с числом пользователей. Расчёты — арифметика, тексты разборов кэшируются навсегда, аркан дня — максимум 22 генерации в сутки на весь сервис, AI-образы собираются из готовых картинок. Чтение оплаченного — всегда без счётчиков. Наценка на кредиты — минимум ×4–×5.

**Нейросети — через KIE** (посредник, оплата из РФ, Anthropic-совместимый endpoint). Тексты разборов — Claude Opus (пишутся один раз, качество важнее цены). Чат — Claude Haiku 4.5. DeepSeek заказчик отверг.

**Название «Моя Эра» оставляем**, проверку по ФИПС пока не делаем.

**Юридические документы** готовят юристы. На сайте остаются заглушки, программисты заменят.

**Ключи и подключение сервисов** — работа программистов, не заказчика. Но передаём им только полностью готовый прототип.

---

## 4. Что уже сделано

Матрица судьбы реализована как флагман:

- Движок расчёта перенесён без изменений из отдельной проработки. Тесты `npm test` зелёные.
- **Постоянные адреса разборов**: `/matrica/13-07-1998`, `/sovmestimost/дата/дата`, `/dengi/дата` и остальные десять типов плюс `/prognoz/`, а с очередей 4 и 5 — `/natalnaya-karta/26-07-1990`, `/dizayn-cheloveka/26-07-1990`, `/numerologiya/26-07-1990` и `/taro/26-07-1990` (124 тысячи страниц, карты сайта в `/sitemap-index.xml`). Страницы строятся по запросу и кэшируются, бесплатные тексты уходят в HTML для индексации. **Ни у одного из тридцати разобранных конкурентов этого нет** — главная возможность проекта.
- Октаграмма из 25 точек с четырьмя слоями и зумом на телефоне. Двенадцать сфер, 92 вопроса, первый в каждой сфере открыт бесплатно.
- Исследовательские блоки с полем даты внутри карточки, «Кого посмотрим дальше?», карта здоровья по чакрам, аркан дня, театр загрузки.
- Supabase: люди, тарифы, подписки, кредиты, тексты, чат — миграции с RLS. Без ключей сайт работает в демо-режиме.
- Кабинет: аркан дня, личный год, календарь месяца, список людей с лимитом тарифа.
- Наставник: потоковый чат через KIE, кредиты, блок безопасности.
- Тарифы, checkout, ЮKassa с вебхуком и идемпотентностью.
- Мобильный первый экран починен, sitemap, robots, OG-картинки, Метрика с восемью событиями. Выдуманные отзывы убраны.

**Инфраструктура настроена:** GitHub → Vercel, автодеплой при пуше в `main`. Сайт живёт на **moya-era.vercel.app** в демо-режиме.

**Очередь 4, шаг 1 сделан (04.09.2026): движок эфемерид.** `src/lib/ephemeris/` на `astronomy-engine`: положения Солнца, Луны и планет, истинный и средний узел, Лилит, дома (Плацидус, Кох, Порфирий, равнодомная, целознаковая), асцендент и MC, звёздное время, поиск Design-даты по 88°. Тесты в `npm test` сверяют движок с эталоном NASA JPL DE421 на 24 датах 1931–2029 (`fixtures/jplDe421.ts`, генератор `scripts/ephemeris-reference.py`). Худшее расхождение 18″ (Нептун), Луна 4″, Солнце 1,3″, при градусе зодиака в 3600″. Дома проверены по определению: асцендент на горизонте, MC на меридиане, Плацидус делит полудуги на трети. В интерфейс расчёт ещё не подключён.

**Очередь 4, шаг 2 сделан (04.09.2026): время и место.** `src/lib/geo/`: справочник мест из GeoNames (18 788 мест: бывший СССР от 500 жителей, остальной мир от 50 000; районы городов отсеяны по коду объекта), русские названия и старые имена как псевдонимы (Ленинград, Свердловск, Горький, Куйбышев), регионы и страны по-русски из таблиц в `scripts/build-places.py`. Подсказки `/api/places?q=`, поле `PlaceField` в профиле и в форме «добавить человека» (там же появилось время рождения). Новая миграция `20260904120000_birth_place_coordinates.sql`: у `profiles` и `people` столбцы `birth_place_id`, `birth_lat`, `birth_lon`, `birth_tz`. Перевод местного времени в UTC (`localTime.ts`) на встроенной базе поясов, проверен тестами на 22 случаях советской истории и на переводах часов. `resolveBirthMoment` даёт точность расчёта: exact / noon (время неизвестно, полдень) / date_only (место не распознано). Источник GeoNames (CC BY 4.0) указан в подвале. Данные выгрузки берутся из npm-пакета `cities-500-structured` (сайт GeoNames из среды Claude недоступен). Крым и Севастополь в подписи показываются без страны («Симферополь, Крым»), потому что GeoNames относит их к Украине; если заказчик хочет иначе, менять в `placesIndex.ts`. На страницах направлений поля времени и места появятся вместе с настоящим результатом в шаге 3.

**Очередь 4, шаг 3 сделан (04.09.2026): натальная карта считается по-настоящему.** `src/lib/natal/`: модель карты (положения по знакам и домам, асцендент и середина неба, аспекты пяти видов с орбисами), разделы разбора и ключи текстов, свои промпты со своей версией `NATAL_PROMPT_VERSION`. Колесо карты `src/components/natal/NatalWheel.tsx` — настоящее: знаки, куспиды домов, планеты на своих градусах, линии аспектов, ретроградность. Страница `/natalnaya-karta` считает по дате, времени и месту; кабинет показывает карту владельца. Тексты идут через тот же контентный слой, что и матрица (таблица `matrix_texts`, ключи с префиксом `natal_`), API `/api/content/natal`, доступ проверяется на сервере. Наполнение базы — `npm run fill-natal-texts` (762 текста, из них 36 бесплатных; отдельный скрипт от матричного, потому что модули на TypeScript и запускаются через tsx, а под tsx перестают читаться матричные `.js`).

**Сверка (шаг 3).** Эталон снят одноразовым скриптом с pyswisseph — той же Swiss Ephemeris, на которой считает astro.com, — и зафиксирован числами в `src/lib/ephemeris/fixtures/swissEphemeris.ts`. Сам скрипт в репозиторий не коммитился, библиотека в зависимости не входит (AGPL). Двадцать контрольных случаев: СССР с декретным и летним временем, 1991 год, южное полушарие, экватор, Мурманск и Норильск. Проверяется вся цепочка: местное время → UTC → планеты, асцендент, MC, дома Плацидуса. Расхождения: планеты до 17″, асцендент до 18″, куспиды домов до 15″, истинный узел до 33″, Design-момент до 19 секунд — при градусе зодиака в 3600″. За полярным кругом Swiss отказывается считать Плацидус, и мы тоже: берём Порфирий и пишем об этом в карте.

**Найдено и исправлено по дороге:** знак Солнца по датам считался с ошибкой — Козерог пересекает Новый год, и все рождённые с 1 по 19 января видели «Рыбы». Баг был в перенесённом коде, теперь закрыт тестом на все дни года.

**Очередь 4, шаг 4 сделан (04.09.2026): дизайн человека считается по-настоящему.** `src/lib/humandesign/`: колесо 64 ворот (отсчёт с ворот 41 на 302°), шесть уровней дробления (ворота, линия, цвет, тон, база), девять центров, 36 каналов, определение центров, тип, стратегия, авторитет, профиль, определение (единое, раздвоенное, тройное), крест воплощения. Настоящий бодиграф `src/components/humandesign/Bodygraph.tsx`: все 36 каналов с разведением параллельных, половины каналов подсвечиваются отдельно. Страница `/dizayn-cheloveka` теперь считает по дате, времени и месту (раньше вела на регистрацию), кабинет показывает бодиграф владельца. Тексты через тот же контентный слой, ключи с префиксом `hd_`, API `/api/content/humandesign`, наполнение — `npm run fill-hd-texts` (339 текстов, из них 10 бесплатных).

**Сверка (шаг 4).** Эталон снят одноразовым скриптом с открытого калькулятора human-design-mcp (MIT, ссылается на Jovian Archive, внутри Swiss Ephemeris), зафиксирован в `src/lib/humandesign/fixtures/openCalculator.ts`. Те же двадцать контрольных дат. Совпало **всё**: Design-момент, ворота и линии всех 26 активаций, набор активных ворот, полные каналы поимённо, определённые центры, тип, авторитет, профиль, определение и ворота креста. Колесо ворот дополнительно сверено с независимым источником: после поворота их последовательность совпала с нашей до последнего элемента.

**Доработка шагов 3 и 4 (04.09.2026): оговорка по факту и уточнение на месте.** Заказчик проверил 13.07.1998 без времени и места: страница показывала тип крупно, как точный результат. Исправлено так:
- `src/lib/geo/dayScan.ts` считает разбор по всем суткам рождения (сетка два часа, момент перехода уточняется делением пополам до минуты). `hdDayVariation` и `natalDayVariation` говорят, меняется ли результат за сутки.
- Если за сутки результат не меняется — оговорок нет вообще, он верен в любой час этого дня. Если меняется — рядом с главным результатом стоит пометка «предварительно», а под ним строка факта: «тип меняется с „Проектор“ на „Генератор“ после 16:00 по времени в городе Москва». Смена типа всегда показывается первой, даже если случилась позже смены профиля.
- `src/components/common/RefineBirth.tsx`: кнопка «Уточнить время и место» раскрывает часы, минуты и поле места прямо в блоке результата. Дату не спрашиваем. После «Уточнить» схема и разбор пересчитываются на месте (в `DirectionPage` у результата появился `update`), пометка исчезает. Вошедшему в аккаунт предлагается кнопка «Сохранить в профиль».
- Формулировка причины подбирается по факту: нет времени при известном месте — полдень по местному времени; место не выбрано — полдень по всемирному.
- Проверено в браузере на 1440 и на 390 пикселей: раскрывающийся блок не ломает вёрстку, горизонтального переполнения нет.

**Очередь 4, шаг 5 сделан (04.09.2026): постоянные адреса карт.**
- Адреса `/natalnaya-karta/26-07-1990` и `/dizayn-cheloveka/26-07-1990` по образцу `/matrica/13-07-1998`. Диапазон дат: с 01.01.1940 по 31 декабря позапрошлого года (сейчас 2024) — 31 047 дат на систему, 62 094 страницы. Всё, что вне диапазона или не существует (31 февраля), даёт 404. Разбор адреса и диапазон — `src/lib/chartUrl.ts`, тесты `src/lib/chartUrl.test.ts`.
- Уточнение — параметрами запроса: `?t=0940&g=524901` (время и id места в GeoNames). Такие адреса закрыты от индексации заголовком `X-Robots-Tag: noindex, follow` из `src/proxy.ts`, canonical у них ведёт на чистый адрес по дате. Причина: дат десятки тысяч, а комбинаций «дата + минута + город» — триллионы.
- Параметры читаются **в браузере** (`src/components/chart/useUrlBirth.ts`), а не на сервере. Если читать `searchParams` в серверном компоненте, страница становится динамической и кэш пропадает. `useSearchParams` тоже не подходит: он выкидывает всё дерево до ближайшего Suspense из готового HTML, а страница существует ради поисковика. Уточнение дописывается в адрес через `window.history.replaceState`, поэтому ссылку с уточнением можно переслать.
- Открыто всем: схема (колесо, бодиграф), положения планет, определённые центры, тип и стратегия, и **короткая справка по каждой позиции** — отдельный текст со своим промптом, а не обрезка платного. Под замком остаётся всё прежнее: полные разборы позиций, дома, аспекты, каналы, ворота, крест.
- Оговорки шага 4 работают и здесь: асцендент и дома на странице по дате не показываются вовсе, а если результат за сутки меняется — рядом стоит «предварительно» и назван час перехода. Кнопка уточнения есть на обеих страницах.
- Кэширование. Страницы строятся по запросу и ложатся в кэш на сутки (ISR). Обязательное условие — `generateStaticParams`, возвращающий **пустой список**: без него Next считает маршрут динамическим и рендерит его заново на каждый заход. Заодно это исправлено и у страниц матрицы, где было так же. Проверено заголовками: первый заход `x-nextjs-cache: MISS`, второй `HIT`, `Cache-Control: s-maxage=86400`.
- Замеры: чистая сборка 22 секунды, при сборке не строится ни одной страницы по дате; холодная страница 27–98 мс, из кэша 3–5 мс. Карты сайта добавляют к сборке 7,6 МБ (4,6 % от 168 МБ).
- Карты сайта: `/sitemap-index.xml` (написан вручную route-обработчиком, потому что `sitemap.ts` умеет только urlset) собирает базовый `/sitemap.xml` и по файлу на систему — `/natalnaya-karta/sitemap/0.xml`, `/dizayn-cheloveka/sitemap/0.xml`. Нарезка по 45 000 адресов: предел поисковиков 50 000, файлов станет больше автоматически, когда дат прибавится. `robots.txt` указывает на индекс.
- Перелинковка: с каждой страницы ссылки на ту же дату в другой системе и в матрице, на соседние дни и на калькулятор. Тридцать тысяч страниц поисковик обойдёт по ссылкам охотнее, чем по карте сайта.
- OG-картинки: `/api/og?chart=natal&date=26-07-1990` — знак Солнца или тип крупно. JSON-LD `Article` с `isAccessibleForFree` на бесплатной части, как у матрицы.
- Попутно исправлено: тексты разбора не сбрасывались при уточнении времени и места — человек уточнял час, а читал разбор прежней карты. Теперь у набора текстов есть подпись из даты, времени и места (`src/components/chart/useSlotTexts.ts`, один хук вместо двух одинаковых).
- Проверено в браузере на 1440 и 390 пикселей: переполнения нет, ошибок в консоли нет, уточнение прямо на странице меняет тип с «Генератор» на «Манифестирующий генератор» и убирает пометку «предварительно».

**Очередь 4, шаг 6 сделан (04.09.2026): страница совместимости — три взгляда на одну пару.**
- Адрес один и тот же: `/sovmestimost/13-07-1998/09-04-1992`, он и индексируется. Три взгляда — матрица, синастрия, композит — живут на нём же и переключаются якорем (`#matrica`, `#sinastriya`, `#kompozit`), без перезагрузки и без повторного ввода дат. Отдельных страниц на взгляд нет намеренно: это были бы три страницы про одну пару, поисковик считает такое дублем.
- Время и место обоих — параметрами запроса `?t1=1720&g1=1496747&t2=1430&g2=524901`, как в шаге 5. Такие адреса закрыты `X-Robots-Tag: noindex, follow`, canonical ведёт на чистый адрес. Даты заново не спрашиваются никогда: они в адресе.
- **Синастрия** (`src/lib/pair/synastry.ts`): межкарточные аспекты и наложение домов. Порядок в паре имеет смысл — «Венера первого и Луна второго» и наоборот это разные конфигурации и разные ключи текстов. Аспекты двух медленных планет не считаются: у ровесников они одинаковы и про пару ничего не говорят. Дома накладываются только когда у обоих известны время и место.
- **Композит** (`src/lib/pair/composite.ts`): бодиграф пары. Каждый горящий канал получает вид связи — электромагнитный (половины у разных людей, канал есть только вдвоём), компаньонство (канал целиком у обоих), доминирование (целиком у одного, у другого ничего), компромисс (целиком у одного, у другого половина). Отдельно считается, откуда взялся каждый центр: у первого, у второго, у обоих или **только вместе** — последнее и есть то, чем пара отличается от двух отдельных людей. Тип и определение пары выводятся из объединённой схемы.
- Бесплатно: схема пары (октаграмма, двойное колесо, бодиграф) и один абзац о главном по каждому из трёх взглядов — свой текст со своим промптом. Остальное под замком. Причина: ссылкой на совместимость делятся со вторым человеком, и он должен увидеть живой результат, а не замок с порога. Матричные бесплатные вопросы («Задача пары», «Ядро союза») остались открытыми, как были.
- Честность прежняя: матрица считается полностью по двум датам, а синастрия и композит без времени рождения обоих неполны — и страница говорит это прямо, по каждому человеку отдельно. У каждого своя карточка с кнопкой уточнения и пересчётом на месте; если за его сутки разбор меняется — «предварительно» и час перехода по московскому времени или по времени указанного места.
- Вошедшему в аккаунт: на форме выбор человека из своих профилей вместо ручного ввода, в разборе — кнопка сохранить второго как нового человека, если в тарифе есть свободное место.
- Карту сайта парами не наполняем: комбинаций дат порядка миллиарда. Страницы пар индексируются по ссылкам, которыми делятся, — ровно так, как ими и пользуются.

---

**Очередь 5, шаг 1 сделан (04.09.2026): нумерология доведена до рабочего состояния.**
- **Методика выбрана одна и записана в коде** (`src/lib/numerology/numbers.ts`): пифагорейская школа по дате, все цифры даты складываются в сумму, сумма сворачивается до одной цифры, свёртка останавливается на 11 или 22. Тридцать три мастер-числом не считаем. Так было задумано с самого начала: одиннадцать текстов чисел пути (1–9, 11, 22) и объяснение в вопросах уже написаны, менять правило значило бы их выбросить.
- **Сверено с двумя независимыми открытыми калькуляторами**, значения зафиксированы числами в `src/lib/numerology/fixtures/calculators.ts`: PyPI `numerology` 1.8 (мастер-чисел не сохраняет вовсе) и npm `@thanhpham99/numerology` 1.0.2, MIT (сохраняет 11, 22 и 33, но складывает свёрнутые по отдельности день, месяц и год). Обе дают другой ответ примерно на одной дате из десяти — и все расхождения ровно про мастер-числа. Тест проверяет две вещи: свёрнутое до 1–9 значение сходится с обоими эталонами на всех датах (это чистая арифметика, от методики не зависит), а каждое расхождение попадает в известный класс. У второго эталона попутно нашёлся дефект: он местами отдаёт 10, то есть не доводит свёртку до конца.
- **Найдена и исправлена ошибка.** Третье рабочее число квадрата Пифагора считалось как «первое минус удвоенная первая цифра дня» без модуля, и для дат двухтысячных уходило в минус: 9 января 2000 года давало «12 − 2×9 = −6». При этом четвёртое число и сам квадрат уже считались по модулю — человек видел одно, а получал другое. Теперь по модулю считается всё. Затронуто 207 дат диапазона, закрыто тестом на все 31 047 дат.
- **Числа разбора:** число жизненного пути, число дня рождения, число отношения (день + месяц) и личный год. Личный год сворачивается без мастер-чисел: это фаза девятилетнего цикла, а не свойство человека, иначе из девяти фаз получилось бы одиннадцать. Плюс квадрат Пифагора, который был у Александры, с четырьмя рабочими числами.
- **Числа судьбы на этом шаге ещё не было** (появилось шагом 3). В пифагорейской школе оно считается по полному имени, а не по дате, а имени мы не спрашиваем. Часть русских школ называет числом судьбы то же самое число жизненного пути; выдумывать ради названия второе число не стали. Если понадобится — это отдельный шаг с полем имени.
- Постоянные адреса `/numerologiya/26-07-1990`, тот же диапазон 1940–2024, та же нарезка карты сайта (`/numerologiya/sitemap/0.xml` в общем индексе), заголовок, описание, OG-картинка и JSON-LD. Времени и места нумерологии не нужно, поэтому уточняющих параметров у этих адресов не бывает и закрывать от индексации нечего.
- Бесплатно: схема из четырёх чисел, квадрат Пифагора, арифметика рабочих чисел и короткая справка по каждому числу. Под замком тринадцать вопросов: четыре числа подробно и девять ячеек квадрата.
- Перелинковка обобщена на три системы: с любой страницы по дате теперь ссылки на две другие и на матрицу.

---

**Очередь 5, шаг 3 сделан (04.09.2026): число судьбы по имени.**
- Поле имени в форме нумерологии — необязательное, рядом с датой: «Полное имя — для числа судьбы. Без него посчитаем всё, что считается по дате». Плюс кнопка «Добавить имя» прямо в результате: разбор пересчитывается на месте, дату заново не спрашиваем. Вошедшему в аккаунт предлагается сохранить имя в профиль.
- **Таблица букв — пифагорейская:** буква получает номер своей позиции в алфавите, свёрнутый до 1–9. Латиница A=1…I=9, J=1…R=9, S=1…Z=8; кириллица — то же правило по русскому алфавиту из 33 букв. Халдейскую таблицу не берём: она устроена по звучанию, без девятки, и смешивать её с пифагорейским счётом дат нельзя. Латинская таблица сверена с теми же двумя открытыми калькуляторами, что и даты, — совпала побуквенно, все 26 значений; открытого калькулятора для кириллицы не нашлось, поэтому вся таблица записана в тесте числами и заперта.
- **Буква Ё приводится к Е.** В печатных таблицах у неё своё значение 7, но «Фёдор» и «Федор» — одно имя, а человек пишет его то так, то этак. Пусть лучше расчёт не зависит от привычки ставить точки, чем совпадает с печатной таблицей ценой двух разных ответов на одно имя.
- **Имя в адрес не кладём.** Кириллица в ссылке превращается в кашу из процентов, а чужое имя в общедоступном адресе — персональные данные. Имя живёт в параметре `?n=`, такая страница закрыта `noindex`, canonical ведёт на чистый адрес по дате. В кэшируемый HTML имя не попадает вовсе — проверено.
- **Обращение по имени** собирается снаружи текста (`greeting`, `afterGreeting` в `letters.ts`), а не подставляется в него: тексты пишутся один раз на всех и лежат в базе, имени в них нет и быть не может. В промпт добавлен явный запрет обращаться по имени и оставлять для него место. Без имени фраза читается сама по себе, без дырки. Этим же местом будут пользоваться письма и телеграм-бот.

---

**Очередь 5, шаг 4 сделан (05.09.2026): имя не уходит в статистику и журналы.** Имя стоит в адресе как `?n=`, а Метрика пишет адрес каждого просмотра — без чистки имена людей осели бы в чужой статистике навсегда. `publicUrl` в `src/lib/chartUrl.ts` снимает личные параметры, и счётчик получает очищенный адрес и в `hit`, и в поле `referer`. `defer: true` в инициализации счётчика обязателен: без него он отправит первый просмотр сам, с настоящим адресом, ещё до нашего вызова.
- Заголовок `Referer` прижат до `strict-origin` — только адрес сайта, без пути. Обычного `strict-origin-when-cross-origin` мало: он режет путь только при уходе на чужой домен, а свои же запросы за картинкой и за шрифтом уносили полный адрес с именем в журнал веб-сервера. Проверено браузером: запрос `favicon.svg` нёс имя, теперь нет.
- На сервер уходит **число** судьбы, а не имя: тексту разбора хватает числа, имя из него не восстановить. В расчёте разделены `destiny` (число) и `destinyBreakdown` (как оно получилось из букв) — у сервера второго просто нет, и это честнее, чем подставлять выдуманную сумму.
- Проверено на собранном сайте: имени нет ни в заголовке вкладки, ни в canonical, ни в OG-картинке, ни в теле запросов, ни в `Referer`, ни в хранилищах браузера. В адресной строке оно остаётся — это браузер человека, так и задумано.
- Попутно найдена и закрыта тестом своя ошибка: значение числа для промпта выводилось цепочкой условий на четыре числа, и пятое, число судьбы, молча получало значение личного года — ключ говорил `num_brief_destiny_8`, а текст писался про личный год 7. Теперь значение спрашивается у карты позиций, источник один на всех.

---

**Очередь 5, шаг 5 сделан (05.09.2026): таро, карта дня.** Постоянный адрес `/taro/26-07-1990`, тот же диапазон дат 1940–2024, своя нарезка карты сайта (`/taro/sitemap/0.xml` в общем индексе), заголовок, описание, OG-картинка и JSON-LD. Живой расклад здесь не показывается — он живёт на `/taro/rasklad` (очередь 3, шаг 5); справочник арканов не делали.
- **Карта детерминированная:** аркан выводится из даты рождения и сегодняшнего дня арифметически. Механика взята из `src/lib/dayCard.ts` — той же, что показывает карту дня в кабинете. Заводить вторую перемешивалку нельзя: карта на странице и карта в кабинете разойдутся, и человек справедливо решит, что мы гадаем.
- **День считается по Москве** (`src/lib/tarot/day.ts`). Иначе двое в разных поясах в один и тот же час увидят разные карты и будут спорить, у кого правильная. Кабинет переведён на ту же карту: раньше он выбирал её по идентификатору пользователя, а не по дате рождения.
- **Выдержка кэша — минута, и это не оплошность.** Next разрешает только постоянное число секунд от постройки страницы, а не от полуночи: час выдержки означал бы час, в который страница показывает вчерашнюю карту. Пересборка — одно чтение из базы и только для запрошенных дат. Вкладку, открытую со вчера, минутой не вылечишь, поэтому страница сама замечает, что московские сутки сменились, и говорит об этом вместо того, чтобы молча показывать вчерашнее.
- Бесплатно: карта, её название и короткая справка. Под замком — «эта карта на фоне вашей карты рождения»: три вопроса, из них один считается по двум арканам сразу, аркану дня и аркану рождения из матрицы (`centralArcanum`). На странице видно, когда карта сменится и что за подпиской карты не пропадают — это будущий повод возвращаться, и бот из очереди 3 будет рассылать именно её.
- **Ключей 550:** 22 справки, 484 пары «аркан дня × аркан рождения», по 22 на «что делать» и «обратную сторону». Перемножается только одна позиция из трёх — если бы все, вышло бы полторы тысячи текстов на одну страницу, и половина не прочиталась бы никогда. Наполнение — `npm run fill-tarot-texts`.
- Витрина `/taro` больше не тянет случайную карту на свободный вопрос: со случайностью карта дня не уживается — если карта меняется от нажатия кнопки, обещание «у всех сегодня одна карта» перестаёт быть правдой в ту же секунду.
- Тяжёлые арканы названы по-нашему: Перерождение, Искушение, Обновление. Названия закрыты тестом и повторены запретом в самом промпте.

**Правка после шага 5 (04.09.2026): время перехода — по московским часам.**
- Раньше без места время перехода называлось «по всемирному времени». Аудитория русскоязычная, и такую подпись человек читает как своё местное время, ошибаясь на три-десять часов. Теперь без места часы московские с пометкой «(мск)»: «тип меняется с „Манифестирующий генератор“ на „Генератор“ после 15:57 (мск)». С местом — часы этого места с его историческим поясом: «после 18:57 (по времени Новосибирска)».
- Вместе с часами сменились и **сутки, которые проверяются**: без места это теперь московские сутки, а не всемирные. Иначе переход в 23:30 по всемирному времени назывался бы «02:30 (мск)» — временем следующего дня. Момент, на который строится разбор, не изменился: полдень по всемирному времени, и он всегда лежит внутри московских суток (это 15:00 или 16:00 мск). В тех же оговорках полдень теперь называется и по-московски, чтобы читатель не складывал двое разных часов.
- Названия городов склоняются в `src/lib/geo/cityName.ts` — но только там, где форма выводится наверняка. Беглая гласная (Череповец, Орёл, Торжок), неопределимый род (Тверь женского, Анадырь мужского), составные названия (Нижний Новгород, Ростов-на-Дону) дают null, и подпись строится безопасно: «(по времени города Ростов-на-Дону)». На ста двадцати крупнейших городах России, Украины, Беларуси и Казахстана правило срабатывает у 91, остальные идут запасным вариантом. Ошибок в падеже нет ни у одного.
- Заодно исправлена оговорка на страницах по дате: если место выбрано, а время нет, страница писала «место не выбрано» и называла чужие часы. Теперь формулировка одна и та же на страницах по дате и на страницах направлений — `whyApproximate`.

**На какое время считается разбор без времени рождения:** полдень. Если место выбрано из справочника — местный полдень этого места с учётом исторического пояса; если места нет — полдень по всемирному времени (по московским часам это 15:00, а в годы летнего времени 16:00; на странице так и написано).

**Объём текстов на генерацию (06.09.2026, после числа дня):** матрица 3520 ключей, натальная карта 894, дизайн человека 357, разбор пары 902, нумерология 178, карта дня 550. Итого **6401**. AI-образы новых ключей не добавляют: карточка собирается из готовых картинок и уже написанных строк смысла. Число дня для бота добавило к нумерологии 18 ключей (девять ступеней × справка и полный разбор).

**Исправление счёта.** Раньше здесь стояло 5833, а потом 6405, и оба числа были завышены на 22: считалось, что число судьбы добавило к нумерологии 22 ключа, а на деле оно с самого начала входило в её 160 — скрипт наполнения перебирает `NUMEROLOGY_NUMBERS` целиком, и destiny там был ещё до того, как появилось поле имени. Верный счёт до числа дня — **6383**. Карта дня: 22 короткие справки, 484 пары «аркан дня × аркан рождения», 22 «что делать» и 22 «обратная сторона»; наполнение `npm run fill-tarot-texts`.

Предыдущий счёт (после нумерологии): матрица 3520, натальная карта 894, дизайн человека 357, разбор пары 902, нумерология 160. Итого **5811**. Число судьбы новых ключей не добавило: оно уже входило в эти 160. Нумерология после числа дня: 62 короткие справки (шесть чисел × их возможные значения), 62 полных разбора тех же чисел и 54 ячейки квадрата (9 цифр × шесть корзин повторов: пусто, 1, 2, 3, 4, пять и больше). Наполнение — `npm run fill-numerology-texts`.

Разбор пары и предыдущий счёт: Разбор пары: 480 межкарточных аспектов синастрии (11 точек в обе стороны, кроме пар двух медленных планет, × 5 аспектов), 144 канала связи (36 каналов × 4 вида связи), 120 наложений домов, 128 бесплатных абзацев «что вас связывает» (22 аркана пары + 80 сочетаний личных планет + 1 на случай, когда аспектов нет + 25 пар типов), 30 пар типов и определений. Наполнение — `npm run fill-pair-texts`.

Предыдущий счёт (после шага 5): матрица 3520, натальная карта 894, дизайн человека 357. Итого **4771**. Короткие справки страниц по дате добавили к прежним 4621 ровно **150** ключей: 132 у натальной карты (11 тел × 12 знаков; ретроградность в ключ не входит — абзац в три строки описывает планету в знаке) и 18 у дизайна человека (9 центров × два состояния). Тип и стратегия своих коротких текстов не получили: они и так открыты целиком. Бесплатных, которые видит посетитель без подписки: 36 + 132 у натальной карты и 10 + 18 у дизайна человека.

**Лилит в карту не входит.** Наш средний апогей расходится со Swiss до семи угловых минут: у них своя серия. Показывать число, которое нельзя защитить, не стали. Функция в движке осталась, вернём, когда сойдёмся с эталоном.

**Очередь 3, шаг 1 сделан (05.09.2026): иллюстрации арканов.** 22 файла JPEG (не PNG, как было написано в коде и документации) в `public/arcana/`, рядом три уменьшенных набора: sm 240 px (28 КБ), md 320 px (47 КБ), lg 560 px (127 КБ), их готовит `scripts/build-arcana.py`. Оригиналы по 334 КБ остаются — из них собираются карточки-талисманы.
- Ступеней три, а не две: с двумя шапка разбора (рамка в 132 точки) тянула файл на 560 пикселей и одна весила больше, чем все остальные картинки страницы вместе. Стало 183 КБ против 262 КБ. Размер файла выбирает сам `ArcanaImage` по ширине рамки, с запасом вдвое ради плотных экранов.
- Адреса собирает `src/lib/arcanaImage.ts`, а не матричный `contentPositions.js`: тот файл редактировать нельзя, и его функция с расширением `.png` теперь никем не зовётся.
- Если картинки нет, на её месте номер и название в рамке того же размера — как было до иллюстраций. В рамках уже 64 точек название не печатается: «Перерождение» там обрезается на середине слова.
- Проверено: сдвиг вёрстки одинаковый с картинками и без (0,0207 и 0,0018) — место резервируется. Страница разбора 604 КБ против 432 КБ до иллюстраций.

**Очередь 3, шаг 2 сделан (05.09.2026): облачко-подсказка.** Полоса внизу страницы разбора, тексты в `src/lib/hints.ts`. Только на страницах разбора, не раньше 90 секунд, только после 10 секунд без прокрутки, не больше трёх за визит с паузой в две минуты, крестик выключает навсегда и на всех страницах, прокрутка убирает сразу.
- Не перекрывает содержимое: в момент появления странице добавляется отступ на высоту полосы И страница поднимается ровно на неё же. Одного отступа мало — он даёт долистать до конца, но поверх середины полоса всё равно легла бы.
- Про подписку подсказка не говорит вовсе: замок и так внутри разбора. Запрет на «осталось N мест», таймеры и зачёркнутые цены написан прямо в файле с текстами.

**Очередь 3, шаг 3 сделан (05.09.2026): AI-образы.** Карточка 1080×1920 со своим арканом, собирается на canvas из готовой иллюстрации. Нейросетью не генерируется: дорого, и стиль плывёт. Подпись с адресом сайта внизу обязательна и галочками не убирается — образ единственный бесплатный канал привлечения в проекте.
- **Семь тем**, и ни одна ничего не считает заново: берут готовое число из матрицы по пути из карты позиций (`core.C`, `core.SE`, `core.SW`, `today.yearArcana`, `purpose.planetary.result`, `chakras.rows.3.physics`) либо карту дня из таро. «Аркан дня» взят из таро — это карта на сегодня, та, что на `/taro/<дата>`, и её же будет рассылать бот; на карточке она подписана «Карта дня».
- **Галочки** перед скачиванием: имя, название аркана, строка смысла — включены; дата рождения выключена, рядом одно короткое предупреждение про восстановление доступа в банке. Выбор запоминается в браузере.
- **Публичная страница** `/obraz/<код>`: карточка и одна кнопка «Рассчитать свою матрицу». Код случайный, десять знаков из алфавита без похожих (ни нуля с «o», ни единицы с «l»). В базе лежит только разрешённое: снял галочку — поле не сохраняется вовсе, и прятать при выводе нечего.
- Таблицу нельзя читать из браузера даже по коду: политика «читать всем» позволила бы выгрузить её целиком, то есть все имена и даты разом. Страница собирается на сервере сервисным ключом.
- Картинку рисует next/og. Шрифты для неё лежат отдельно в ttf (`scripts/build-og-fonts.py`): Satori не понимает woff2. Превью для мессенджера — копия в 480 точек: WhatsApp пропускает предпросмотр примерно от полумегабайта (полная карточка 2,1 МБ, в 480 точек — 456 КБ).
- **Доступ:** первая карточка бесплатно всем и без регистрации, дальше по подписке. Кредиты не тратятся — расхода на нейросеть нет. Отметка о бесплатной лежит в cookie: у незарегистрированного человека больше ничего нет, а подделать её несложно и пусть — это канал привлечения, а не замок.
- Кнопка «Сделать образ» в разборе рядом с центральным арканом, в нумерологии, на странице карты дня и в кабинете.

**Очередь 3, шаг 4 сделан (06.09.2026): телеграм-бот.** Живёт внутри этого же проекта: общая база, общие тексты, программистам уходит один репозиторий. Приём сообщений — вебхуком на `/api/telegram/webhook`, рассылка — задачами Vercel. Нет токена — бот выключен, и сайт работает как обычно.
- **Привязка** ссылкой `t.me/<бот>?start=КОД` на профиль человека. Первый нажавший занимает её, остальным отказ; владелец может отозвать — код гаснет навсегда и чат отписывается. Лимит по тарифу: Базовый 1, Семейный 5, Практик 15; на число профилей на сайте это не влияет. Люди заводятся только в кабинете: в боте формы с датой, временем и местом нет и не будет.
- **Ссылку занимает условный запрос** (`chat_id is null` прямо в UPDATE), а не чтение с последующей записью: два одновременных «Старта» иначе были бы гонкой с ценой в чужую рассылку.
- **Сводка** утром около восьми по поясу человека: аркан дня из матрицы, карта дня из таро, число дня из нумерологии. Направления включаются кнопками; выключил все — сообщение не уходит. Астрологии нет и не будет, пока не считаем транзиты.
- **Правило отправки догоняющее**, а не оконное: слать, если по местным часам уже восемь, а сегодняшняя не уходила. Оконное («кому сейчас ровно восемь») теряет пояса со сдвигом на полчаса и всех, чей запуск не состоялся.
- **Тексты только из общего кэша, теми же ключами, что и сайт.** `botDigest` намеренно не зовёт `getTexts`: та умеет генерировать. Готовит тексты отдельная задача заранее, до рассылки. Ключей на сутки 53, из них новых каждый день ровно 22 — аркан дня выводится из аркана периода, поэтому больше и не бывает, сколько бы ни было подписчиков.
- **Число дня** пришлось добавить в саму нумерологию (`personalDay`, своя позиция разбора, девять ключей в общем скрипте наполнения): на сайте его не было, а заводить боту собственные тексты нельзя.
- 403 от телеграма — это состояние, а не сбой: привязка помечается заблокированной и больше не берётся в очередь, иначе мёртвые чаты съели бы время запуска.
- **Архив сводок** остаётся у человека навсегда: отдельная таблица, не привязанная к строке привязки и переживающая удаление профиля.
- О боте человек узнаёт в одном месте — панель в кабинете. Тем, кто пришёл только в бота, кнопка «Посчитать свой полный разбор» показывается не чаще раза в десять сводок.
- **Вживую не проверен: нужен токен.** Что закрыто тестами и что осталось на живую проверку — в `docs/BOT.md`, там же по шагам всё, что нужно программистам.

**Очередь 3, шаг 5 сделан (06.09.2026): живой расклад таро.** Четыре вида на 22 старших арканах: одна карта (1 кредит), три карты — ситуация, препятствие, совет (3), отношения, пять карт (4), большой расклад, десять карт (7). Младшие арканы не заводим: без иллюстраций они выглядели бы бедно рядом с остальным сайтом.
- **Цены лежат в базе**, в `spread_types`, рядом с тарифами — меняются UPDATE-ом без деплоя. В коде остаются запасные: пропавшая строка не должна делать расклад бесплатным. Тест сверяет запасные цены с засеянными в миграции.
- **Карты тянет сервер криптостойким источником** (`crypto.randomInt`), перемешиванием Фишера — Йетса. На клиенте раздача была бы и подделываема, и предсказуема, а человек за неё заплатил. Карта может лечь перевёрнутой — тогда разворачивается изображение, подпись остаётся читаемой.
- **Кредиты — той же механикой, что у наставника.** Общая `src/server/credits.ts`: списать → сгенерировать → не вышло → вернуть. Чат наставника переведён на неё же, второй реализации нет. Для расклада добавлена `spend_credits` (несколько кредитов в одной транзакции под тем же advisory-локом): семь вызовов `spend_credit` были бы семью транзакциями, и на четвёртой мог бы кончиться баланс.
- **Расклад сохраняется навсегда** — свой адрес `/rasklad/<код>` и раздел в кабинете. Остаётся и после отмены подписки, как архив сводок: подписка открывает новые расклады, купленное остаётся купленным.
- **Страница закрыта от индексации тремя рубежами**: `X-Robots-Tag` в заголовке ответа, `noindex` в метаданных, `Disallow: /rasklad/` в robots.txt. За адресом — вопрос, который человек часто не задавал никому живому. Смотреть расклад может только тот, кто его сделал: ссылку можно переслать по ошибке.
- **Из корневого макета убрана строка `robots: { index: true, follow: true }`.** Этот Next не заменяет её тем, что просит страница, а дописывает второй тег рядом: страницы с `noindex` (наставник, оплата, расклад) выходили с двумя противоречащими указаниями. Проверено на сборке, а не в dev.
- **Блок безопасности перенесён из промпта наставника целиком и дословно.** Копия, а не ссылка: оригинал в закрытом `lib/matrix/prompts.js`. Тест сверяет её с `MENTOR_SYSTEM_PROMPT` посимвольно — сокращение блока валит сборку. Сюда приходят с тяжёлыми вопросами чаще, чем куда-либо ещё на сайте.
- **Расклад объясняет, как посмотреть на ситуацию, а не что случится.** Предсказаний событий, сроков, сумм, диагнозов и денежных советов промпт запрещает прямым текстом: от этого зависит и приём платежей, и реклама.
- **Новых ключей текстов расклады не добавляют: ноль.** Текст пишется под вопрос человека одним запросом и хранится в самом раскладе; в общий кэш `matrix_texts` он не попадает и попасть не может — вопрос у каждого свой. Итог по системам остаётся **6401**.
- Без подписки расклады недоступны — это платная механика, а не витрина. Бесплатное в таро уже есть: карта дня.
- **Вживую не проверено: нужны база и ключ нейросети.** Списание и возврат кредитов, сохранение расклада, страница по коду и история в кабинете на живых данных — в приёмку.

**Очередь 3, шаг 6 сделан (06.09.2026): автопродление подписки.** Задача по расписанию `/api/cron/renew` (раз в сутки на бесплатном тарифе Vercel, на Pro — каждые шесть часов). **Выключена по умолчанию**: без переменной `BILLING_AUTO_RENEW` не делает ничего. До появления оферты с условиями автосписания включать нельзя — это требование и закона, и банка.
- **Порядок:** за три дня предупреждение (сумма, день, как отключить) → за день до конца платёж по сохранённому способу оплаты → не прошло: повтор через день, потом ещё через три → три неудачи: честное сообщение и бесплатный режим.
- **Дважды не спишет.** Попытка занимается в базе (`claim_renewal`, UNIQUE на пару «подписка + конец периода») ДО похода в банк, и у каждой попытки свой ключ идемпотентности. Повторный запуск получает от ЮKassa тот же платёж вместо второго списания. На Pro задачу стоит пускать каждые шесть часов: ключ идемпотентности ЮKassa живёт сутки, и висящий платёж (банк не ответил, потребовалось подтверждение) надо успеть переспросить; на бесплатном тарифе возможен только суточный запуск. Уже созданный платёж переспрашивается по номеру, а не создаётся заново.
- **Обрыв сети и 5xx считаются висящим платежом, а не неудачей.** Неудача назначила бы новую попытку с новым ключом, и если первый запрос всё-таки дошёл, человека списали бы дважды.
- **Кнопка «Отключить автопродление» в кабинете**, в блоке подписки, видная. Выключение ничего не отменяет: доступ живёт до конца оплаченного периода, кредиты остаются на счету, сделанные расклады остаются на своих адресах. Когда механика выключена переменной, кнопки нет вовсе — панель прямо говорит, что подписка не продлится: выключатель того, чего не происходит, это обман.
- **Предупреждение постоянное, а не разовое.** Живой канал у нас один — телеграм-бот, и он есть не у каждого. Поэтому та же строка (что спишется, когда, сколько) стоит в кабинете всегда. Почтовой рассылки в проекте нет вовсе — это в приёмку.
- **Нижняя граница выборки обязательна.** Статус подписки после конца периода сам собой не меняется (доступ считается по дате), и без нижней границы задача забрала бы все давно кончившиеся подписки разом и списала бы с них деньги при первом запуске. Окно — шесть дней от конца периода; закрыто тестом, который падает при снятии границы.
- **Границу периода считает `addMonths` с якорным днём.** Обычный `setMonth` из 31 января делает 3 марта. Простое прижатие к концу месяца тоже неверно: 31 января → 28 февраля → 28 марта, и день оплаты навсегда уезжает на три дня. Якорь — день первой оплаты. Тем же `addMonths` теперь считается и первый период при оплате: раньше там был `setMonth` с тем же перетеканием.
- **Починена причина `spread` в журнале кредитов.** Ограничение `credits_ledger.reason` писалось до раскладов, и списание за расклад в него не проходило: при живой базе расклады не работали бы вовсе. Список причин вынесен в `src/lib/creditReasons.ts`, тип у `spendCredits` теперь не `string`, а тест сверяет список с последним CHECK в миграциях.
- **Вживую не проверено: нужны настоящие платежи.** Что именно осталось — в README и в отчёте по приёмке.

**Сквозной проход по сайту (06.09.2026): что нашлось и что починено.** Обход всех страниц на 1440 и 390 точек, все внутренние ссылки (47 адресов, все отвечают 200), поиск по коду. Починено:
- **Наставник в демо-режиме не работал вовсе**: каждое сообщение — 500. Причина — символ «∞» в HTTP-заголовке `X-Credits-Left`: заголовки принимают только латиницу-1, Node бросал TypeError. В демо заголовок больше не отдаётся.
- **Переполнение 12px на всех разборах матрицы (телефон)** — таблица чакр с `min-w-[340px]` распирала колонку сетки. Убрана минимальная ширина, колонкам добавлен `min-w-0`. **Наставник на телефоне уезжал на 31px** — выпадающий список и поле ввода со своей минимальной шириной; те же `min-w-0`.
- **Предупреждение о гидратации на каждой странице.** Три причины: звёзды на карточках направлений считались `Math.random()` при рендере (сервер и браузер получали разные координаты — теперь генератор с зерном, `src/lib/seededRandom.ts`); координаты колеса направлений с пятнадцатью знаками после запятой (браузер хранит стили до сотых — округлены); `Math.cos/sin` у Node и браузера расходятся в последнем знаке — координаты октаграммы и натального круга округлены до тысячных.
- **Матрица строилась на будущую дату в текущем году** (`/matrica/31-12-2026` отдавала полный разбор с заголовком и OG). Проверка «год не больше текущего» заменена проверкой по дню — в адресах и во всех трёх калькуляторах; калькуляторы теперь говорят «Эта дата ещё не наступила», а поле из трёх списков — ещё и «Такой даты не существует» вместо молча отключённой кнопки.
- **Превью ссылок**: страницы направлений, тарифов, документов и входа уходили в мессенджеры без картинки — свой `openGraph` заменяет родительский целиком и теряет файловую картинку. Добавлена общая `DEFAULT_OG_IMAGE` (`src/lib/seo.ts`) на 17 страницах.
- **Мелкие цели для пальца**: строки вопросов на страницах натальной карты, дизайна человека, нумерологии, пары и карты дня были кнопками высотой 21px — отступ переехал внутрь кнопки, теперь 53px без изменения вида. «Забыл пароль» на входе — 36px вместо 20.
- **Подвал вёл в никуда**: `hello@moyaera.ru` (домена не существует) и голая ссылка на t.me. Контакты теперь из переменных `NEXT_PUBLIC_CONTACT_EMAIL` и `NEXT_PUBLIC_TELEGRAM_URL`, пока не заданы — не показываются.
- Мелочи: заголовок главной «Моя.Эра» приведён к «Моя Эра»; на странице пары было два h1 (встроенный разбор матрицы теперь h2); `/checkout` без параметров отправлял на регистрацию с `next=/checkout?`.
- **Проверено и в порядке**: битых картинок нет, alt у всех; заголовок и описание есть у каждой страницы; имя из `?n=` не попадает ни в заголовок, ни в OG, ни в canonical; тяжёлые арканы названы по-нашему везде, кроме закрытого `matrix/prompts.js` (оттуда в интерфейс не выводятся); зачёркнутых цен, таймеров и «осталось N мест» нет; обещаний событий в текстах интерфейса нет. Пустые состояния кабинета (нет даты, людей, подписки, раскладов) объясняют, что делать; место не из справочника сохраняется как текст с честным предупреждением. Скорость на сборке: страница разбора строится за 20–180 мс, из кэша отдаётся за 3–10 мс, OG-картинка — 260 мс.
- **Требует решения заказчика (не трогал)**: шесть отзывов на главной с именами и фотографиями при отсутствии клиентов; какие почту и телеграм ставить в подвал; текст «напишите нам» на странице после оплаты при отсутствии контактов; юридические заглушки и реквизиты ИП в подвале.

**Почему живой сайт отстал от `main` (05.09.2026).** С коммита бота в `vercel.json` стояли расписания «каждые десять минут» и «каждые шесть часов». Бесплатный тариф Vercel принимает только ежедневные — деплой падал целиком, и сайт застыл на версии до бота. Расписание переведено на раз в сутки (`warm` 01:00, `renew` 06:00, `digest` 07:00 UTC); час рассылки выбран так, чтобы догоняющее правило покрыло все пояса бота за один проход (Берлин зимой — UTC+1). Закрыто тестами, в том числе сверкой `vercel.json` с константой. Частое расписание для Pro описано в `docs/BOT.md`. Заодно: блок отзывов удалён из кода целиком (компонент и портреты) — клиентов нет, отзывов нет; «напишите нам» после оплаты показывается только при заданных контактах.

---

## 5. Что дальше

**Очередь 3 — механики возврата. Закрыта 06.09.2026.** Ради них всё и затевалось, без них ежемесячная подписка не живёт: человек прочитал разбор за вечер и отменил.

- Иллюстрации арканов: получены от заказчика и подключены — **сделано**.
- Облачко-подсказка: только на страницах разбора, не раньше 90 секунд, только когда человек остановился, максимум три за визит, крестик закрывает навсегда — **сделано**.
- AI-образы: карточка собирается на canvas из готовых иллюстраций (не генерируется нейросетью — дорого и стиль плывёт), семь тем, публичная страница, первый образ бесплатно — **сделано**.
- Telegram-бот: утренняя сводка, тексты из той же базы, что и сайт — **сделан**, но вживую не проверен: нужен токен (`docs/BOT.md`).
- Живой расклад таро: четыре вида, свои цены в базе, кредиты общей механикой с наставником, свой адрес и история в кабинете — **сделан**.
- Автопродление подписки: задача по расписанию, повторы, кнопка отключения в кабинете — **сделано**, но выключено переменной `BILLING_AUTO_RENEW` до появления оферты с условиями автосписания. Вживую не проверено: нужны настоящие платежи. **Очередь 3 закрыта.**


**Очередь 4 — остальные пять систем. Закрыта 04.09.2026.**

Эфемериды: **`astronomy-engine` (MIT)**. Заменила `libephemeris` из первоначального плана: та оказалась библиотекой на Python 3.12+ и под **AGPL-3.0-only** (Apache-2.0 стояла по ошибке только в пре-релизах rc2–rc8, они отозваны с PyPI). Swiss Ephemeris, любые обёртки над ним (в том числе WASM) и Moshier на JS под запретом: AGPL/GPL. `astronomia` (MIT) допустима только как перекрёстная проверка в тестах, в прод не тащить. Точность astronomy-engine сверена с NASA JPL DE421: худшее расхождение 18″ при градусе зодиака в 3600″ и линии дизайна человека в 3375″. Библиотека без файлов данных, 116 КБ, считает и на сервере, и в браузере — значит, бесплатный расчёт на странице дизайна человека возможен (в брифе его закрыли, потому что «в браузере не считается»).

Часовые пояса: встроенная база Node (tzdata) проверена на советской истории по одиннадцати городам СССР: декретное время 1930, летнее время 1981–1991, отмена декретного в 1991 и возврат в 1992, реформы 2011 и 2014, местное среднее время до 1930. Отдельная библиотека не нужна. Нужен справочник мест: GeoNames офлайн (CC BY 4.0, источник указать на сайте), русские названия и поиск по русскому вводу, Россия и СНГ подробно, остальной мир — крупные города. Из песочницы Claude сайт GeoNames не качается (прокси даёт 403), брать через зеркало на GitHub или просить заказчика разрешить домен.

Дизайн человека: Design-дата ищется численно как момент, когда Солнце было на **88 градусов долготы раньше**, а не «88 дней назад»: разброс реально 87–92 суток (проверено: 26.07.1990 → 25.04.1990, 91,8 суток).

Утверждённый бесплатный объём (04.09.2026), по принципу матрицы: схема бесплатна, толкования по подписке, замок внутри разбора, а не перед ним.
- Натальная карта: круг карты с положениями планет виден всем без регистрации. Бесплатно читаются Солнце (по дате) и Луна с асцендентом (если введены время и место). Остальные планеты, дома, аспекты — подписка.
- Дизайн человека: бодиграф с определёнными центрами виден всем. Бесплатно — тип и стратегия. Авторитет, профиль, каналы, ворота — подписка.
- Время рождения неизвестно: считать на 12:00 и честно показывать, что асцендент, дома и всё, что от них зависит, не определены. Ничего не выдумывать.

Шаги очереди. По одному, каждый — отдельный коммит, следующий только после «дальше» от заказчика:
1. Движок эфемерид `src/lib/ephemeris/`, тесты против JPL — **сделан**.
2. Время и место: справочник городов, подсказка места в кабинете, новая миграция (координаты, пояс), перевод местного времени в UTC с историей поясов — **сделан**.
3. Натальная карта: модель карты, колесо с домами и аспектами, кабинет, калькулятор, вопросы и тексты — **сделан**.
4. Дизайн человека: ворота, каналы, центры, тип, стратегия, авторитет, профиль, крест, настоящий бодиграф, калькулятор, кабинет, тексты — **сделан**.
5. Постоянные адреса и SEO для обеих систем, OG-картинки, sitemap — **сделан**.
6. Страница совместимости: одна форма, три взгляда на пару (матрица, синастрия, композит) на одном адресе — **сделан**. Очередь закрыта.

Перед шагами 3 и 4: сверить весь расчёт целиком (положения планет, асцендент, дома) с astro.com или другим независимым источником минимум на 20 контрольных датах, обязательно включая рождения в СССР (декретное время, летнее время 1981–1991, чехарда 1991 года). Для дизайна человека на тех же датах сверить тип, авторитет, профиль и Design-дату с открытым калькулятором. Результаты сверки зафиксировать в тестах.

**Очередь 5 — нумерология и таро. Закрыта 05.09.2026.** Шаги: нумерология до рабочего состояния — **сделан**; разведка по таро (предложены варианты, заказчик выбрал карту дня) — **сделан**; число судьбы по имени — **сделан**; имя не уходит в статистику и журналы — **сделан**; таро, карта дня — **сделан**.

**Что дальше по таро.** Живой расклад сделан (очередь 3, шаг 5): четыре вида, вопрос своими словами. Справочник арканов сознательно не делали. Карта дня уже готова к рассылке ботом из очереди 3: механика детерминированная, день считается по Москве, текст лежит в общей базе.


**Очередь 6 — рост.** Граф «мои люди», справочник с отдельной страницей на каждую сущность, фасетные теги, PDF, вход через VK/Google, гарантия возврата.

---

## 6. Правила, которые нельзя нарушать

- `src/lib/matrix/matrixEngine.js` и `contentPositions.js` **не редактировать**. Методика сверена с четырьмя внешними сервисами до последней цифры, тесты — контроль. Заказчик сказал: методика закрыта.
- Идентификаторы вопросов (`slot.id`) не переименовывать — на них висят тексты в базе.
- Меняешь промпты — поднимай `PROMPT_VERSION`, иначе старые и новые тексты смешаются.
- **Никаких зачёркнутых цен, таймеров обратного отсчёта и «осталось N мест».** Весь рынок работает на фальшивом дефиците, мы принципиально нет — это и позиция продукта, и условие эквайринга.
- Тексты нигде не обещают предсказание событий. От этого зависит и приём платежей, и реклама.
- Тяжёлые арканы переименованы: Смерть → Перерождение, Дьявол → Искушение, Башня → Обновление. Названия закрыты тестом (`src/lib/tarot/tarot.test.ts`) и повторены запретом внутри промпта карты дня.
- **Имя человека не покидает его браузер и его профиль.** В Метрику уходит очищенный адрес (`publicUrl`), `Referer` прижат до `strict-origin`, на сервер отправляется число судьбы, а не имя. Заводя новый личный параметр адреса, впиши его в `PRIVATE_PARAMS` — там же лежит и объяснение, почему время и место туда сознательно не входят.
- **Подпись с адресом сайта внизу карточки образа не убирается галочками.** Образ — единственный бесплатный канал привлечения: карточку выкладывают, и по ней должны приходить.
- **У бота нет и не должно быть своих текстов.** Только общий кэш и те же ключи, что у сайта: иначе расход на нейросеть из фиксированного станет пропорциональным числу подписчиков, а сводка разойдётся с кабинетом.
- **Карта дня считается по московским суткам.** Если день определять по часам сервера или читателя, двое в разных поясах увидят разные карты в один и тот же час. По той же причине выбор карты детерминированный: случайность здесь ломает само обещание.
- Блок безопасности в промпте наставника (что делать при признаках тяжёлого состояния человека) не сокращать и не переписывать.
- Эфемериды только `astronomy-engine` (MIT). Swiss Ephemeris, обёртки над ним, Moshier-JS и `libephemeris` под запретом: AGPL/GPL.
- Правила очереди 4 (от заказчика): один шаг — один коммит, после шага остановиться и ждать «дальше»; перед коммитом `npm test` и `npm run build` зелёные; коммит и пуш сразу в `main`, Vercel выкатывает сам; миграции только новыми файлами; ключи, Supabase и платежи не настраивать — это сделают программисты.

---

## 7. Где что искать

| Что | Где |
|---|---|
| Устройство проекта, запуск, деплой | `README.md` |
| Настройка сервисов и чек-лист приёмки | `docs/PEREDACHA-PROGRAMMISTAM.md` — **отдавать программистам только когда сайт готов**, заказчик просил не торопиться |
| Движок матрицы и карта позиций | `src/lib/matrix/` |
| Движок эфемерид: планеты, узлы, дома, Design-дата | `src/lib/ephemeris/`, тесты `ephemeris.test.ts`, эталон JPL `fixtures/jplDe421.ts`, генератор `scripts/ephemeris-reference.py` |
| Место рождения и часовые пояса: справочник, поиск, перевод времени в UTC, момент рождения | `src/lib/geo/`, данные `data/places.json`, генератор `scripts/build-places.py`, API `src/app/api/places/`, поле `src/components/common/PlaceField.tsx` |
| Натальная карта: модель, разделы, промпты, сверка со Swiss | `src/lib/natal/`, колесо `src/components/natal/`, сервер `src/server/natalTexts.ts`, эталон `src/lib/ephemeris/fixtures/swissEphemeris.ts` |
| Нумерология: числа по дате, число судьбы по имени, квадрат Пифагора, промпты | `src/lib/numerology/`, буквы `letters.ts`, сервер `src/server/numerologyTexts.ts` |
| Карта дня: выбор аркана, московские сутки, позиции разбора, промпты | `src/lib/tarot/`, общая перемешивалка `src/lib/dayCard.ts`, сервер `src/server/tarotTexts.ts` |
| AI-образы: семь тем, разметка карточки, коды адресов | `src/lib/image/`, отрисовка `render.ts`, окно `src/components/image/`, хранилище `src/server/imageStore.ts` |
| Иллюстрации арканов и подсказки | `public/arcana/` + `src/lib/arcanaImage.ts`, тексты подсказок `src/lib/hints.ts` |
| Разбор пары: синастрия и композит | `src/lib/pair/`, сервер `src/server/pairTexts.ts` |
| Постоянные адреса по дате, диапазон, карты сайта, личные параметры адреса | `src/lib/chartUrl.ts`, noindex уточнённых адресов — `src/proxy.ts` |
| Дизайн человека: ворота, каналы, центры, тип, бодиграф | `src/lib/humandesign/`, схема `src/components/humandesign/`, сервер `src/server/hdTexts.ts`, эталон `fixtures/openCalculator.ts` |
| Страницы разборов | `src/app/[type]/[...dates]/` |
| Интерфейс разбора | `src/components/reading/` |
| Серверная логика | `src/server/` |
| Схема базы | `supabase/migrations/` |

Полные документы — аудит на 45 пунктов, разбор тридцати конкурентов, транскрипт голосовых от руководителя, журнал решений — лежат в проекте заказчика «Сайты» в Claude. Если они недоступны, этого файла достаточно, чтобы продолжить работу.

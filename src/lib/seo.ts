/**
 * ОБЩЕЕ ДЛЯ МЕТАДАННЫХ СТРАНИЦ.
 *
 * Картинка для превью ссылки по умолчанию — та, что собирает
 * `app/opengraph-image.tsx`. Файловая картинка достаётся только корневому
 * сегменту: страница, которая задаёт свой `openGraph` (заголовок,
 * описание), заменяет им родительский целиком и картинку теряет. Так
 * страницы направлений, тарифов и документов уходили в мессенджеры без
 * превью. Поэтому каждая страница, у которой нет своей картинки,
 * вписывает эту.
 */
export const DEFAULT_OG_IMAGE = { url: "/opengraph-image", width: 1200, height: 630 };

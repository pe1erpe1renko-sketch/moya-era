/**
 * AI-ОБРАЗЫ — карточка со своим арканом, которую человек выкладывает.
 *
 * Собирается на canvas из готовых иллюстраций арканов. Нейросетью НЕ
 * генерируется: дорого, и стиль плывёт от карточки к карточке.
 *
 * Это единственный бесплатный канал привлечения в проекте, поэтому
 * подпись с адресом сайта внизу карточки обязательна и галочками не
 * убирается.
 */

export * from "./themes";
export * from "./layout";
export * from "./choices";
